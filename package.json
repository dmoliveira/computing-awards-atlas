{
  "name": "computing-awards-atlas",
  "version": "0.1.0",
  "private": true,
  "description": "Static GitHub Pages atlas for major computing awards, laureates, and related works.",
  "scripts": {
    "dev": "npm run build:data && next dev",
    "build:data": "node scripts/build-data.mjs",
    "build": "npm run build:data && next build",
    "assert:pages": "node scripts/assert-pages-build.mjs",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "validate": "npm run build && npm run typecheck && npm run lint && npm run test",
    "validate:pages": "STATIC_EXPORT=true npm run build && npm run assert:pages && npm run typecheck && npm run lint && npm run test"
  },
  "dependencies": {
    "fuse.js": "^7.1.0",
    "next": "16.2.6",
    "react": "19.2.6",
    "react-dom": "19.2.6"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "eslint": "^9.39.1",
    "eslint-config-next": "16.2.6",
    "typescript": "6.0.3"
  }
}
